<h1><%= @dog.name %></h1>

<h3>
	Breed: <%= @dog.breed %><br>
	Age: <%= @dog.age %><br><br>

	Owners:
	<ul>
		<% @dog.employees.each do |e| %>
			<li><%= link_to e.to_s, employee_path(e) %></li>
		<% end %>
	</ul>
</h3>

<% @model = @dog %>
<%= render 'layouts/show_links' %>